import React from "react";

const Login = (props) => {
  // console.log(props, 'Login');

  return (
    <div style={{ textAlign: "center" }}>
      <p>login page text ...</p>
      ...
    </div>
  );
};

export default Login;
